language: ruby
cache: bundler
archive: true
rvm:
  - 2.1.2
before_script:
  - cd spec/dummy
  - RAILS_ENV=test bundle exec rake db:migrate
  - cd ../..
script:
  - CODECLIMATE_REPO_TOKEN=123 bundle exec rspec
notifications:
   email: false
